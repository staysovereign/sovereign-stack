# Synapse homeserver configuration for Sovereign
# Generated by setup.sh — edit SOVEREIGN_MATRIX_SERVER_NAME and secrets only.
#
# This config is intentionally minimal:
#   - No federation (Sovereign is a private deployment)
#   - SQLite database (sufficient for single-user)
#   - Registration disabled (you create the sovereign bot user via setup.sh)

server_name: "SOVEREIGN_MATRIX_SERVER_NAME"
pid_file: /data/homeserver.pid

listeners:
  - port: 8008
    tls: false
    type: http
    x_forwarded: true
    resources:
      - names: [client, federation]
        compress: false

database:
  name: sqlite3
  args:
    database: /data/homeserver.db

log_config: "/data/log.config"
media_store_path: /data/media_store

# Secrets — filled in by setup.sh, do not commit
registration_shared_secret: "SOVEREIGN_REGISTRATION_SHARED_SECRET"
macaroon_secret_key: "SOVEREIGN_MACAROON_SECRET"
form_secret: "SOVEREIGN_FORM_SECRET"

signing_key_path: "/data/signing.key"

# Federation off — this is a private bridge node
federation_domain_whitelist: []
trusted_key_servers: []
suppress_key_server_warning: true

# No public registration — sovereign bot user created via setup.sh
enable_registration: false
allow_guest_access: false
enable_search: false

# Application services (WhatsApp bridge)
app_service_config_files:
  - /data/appservices/whatsapp-registration.yaml

# Reduce noise in a single-user deployment
report_stats: false
enable_metrics: false
